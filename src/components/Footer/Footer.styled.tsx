import styled from 'styled-components/macro';

export const StyledFooter = styled.footer`
  padding: 3rem;
  background-color: black;
  

  a, a:visited, a:focus, a:hover, a.active { color: white; margin-bottom: 2rem; }
`;
